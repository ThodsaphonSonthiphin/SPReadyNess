# Reflections — 2026-07

## 2026-07-26 — first build/run of SPReadyNess, UI debugging session

**Project:** SPReadyNess (this repo)

**Findings:**
- [Route F · GOTCHAS.md] `brew install --cask` needs interactive sudo, fails in agent Bash — prefer non-cask formula.
- [Route C · CLAUDE.md] Connect IQ SDK 9.2.0 gotchas cluster (drawText anchor, Storage Symbol-key ban, assertEqualMessage null bug, `(:test)` auto-harness collision, monkeydo exit code, AppBase override tuple types).
- [Route F · GOTCHAS.md] No screen-capture ability in this harness — ask user for screenshots.
- [Route F · GOTCHAS.md] Mid-session-cloned project skills may not register with `Skill()` immediately.
- [Route C · CLAUDE.md] No scriptable simulator input — visual verification needs manual user navigation each round.

**Applied:** all 5 (user approved "all").

**Sources cited:** all findings verified empirically within-session (live `brew`/`screencapture`/`osascript`/`monkeyc`/`monkeydo` runs and SDK `api.mir` type definitions) — no external doc/GitHub research needed (no Route B new-skill candidates this session).

**Note:** this skill's Stage 5 instructions assume a "plugin repo" (dev-workflows-style marketplace) that doesn't exist in this project — no `.claude-plugin/marketplace.json` found here. Filed this record in this repo's own `docs/reflections/` as the fallback.

## 2026-07-26 — RHR-missing/crash debug session (grill-then-plan → debug-mantra → scrutinize)

**Project:** SPReadyNess (this repo)

**Findings:**
- [Route F · GOTCHAS.md] Checking plugin-skill registration (e.g. `superpowers`) via `~/.claude/plugins/`/`~/.claude.json` archaeology is slow/inconclusive — calling `Skill()` directly settles it in one shot.
- [Route C · CLAUDE.md] SDK is only "on PATH" in an interactive shell — Claude Code's Bash tool doesn't inherit it and doesn't persist exports between calls; every command needs an inline `export PATH` with the resolved SDK dir.
- [Route C · CLAUDE.md] `./verify.sh` / bare `monkeydo` fail with "Unable to connect to simulator" unless `connectiq &` is already running — not documented as a `verify.sh` prerequisite.
- [Route D · memory] Ranked a hypothesis as "most likely, explains both symptoms" while its crash-explaining half rested on an unverified Monkey C exception-semantics assumption — caught only via `/scrutinize`. Filed as a feedback memory on separating confirmed trace from assumed mechanism.

**Applied:** all 4 (user approved "fix all").

**Sources cited:** Route F/C findings verified empirically in-session (live `Skill()` call, live `monkeyc`/`monkeydo`/`connectiq` runs). Route D finding's underlying technical question (do Connect IQ permission violations bypass try/catch?) was researched via WebSearch/WebFetch (Garmin forums, developer docs) and remains **unconfirmed either way** — the memory records that as the lesson, not a settled answer.

**Still open (not a reflect finding, tracked in-conversation):** root cause of the real-device crash and missing RHR is unresolved pending the user pulling `GARMIN/APPS/LOGS/CIQ_LOG.YAML` from the watch after a reproduced crash.

## 2026-07-26 — RHR data-source redesign: grill-then-plan → subagent-driven-development → ship

**Project:** SPReadyNess (this repo)

**Findings:**
- [Route D · memory] Plan sequencing bug: Task 3's brief said delete `Sensors.rhr()`, but Task 4 removed its last caller — implementer correctly reported BLOCKED instead of guessing, requiring a mid-execution plan fix.
- [Route D · memory] Fixed an unbounded sensor-history walk for the "after wake" call state via `/scrutinize`, but missed the identical bug in the "before wake" state — only the independent final-whole-branch-review subagent caught it. Two of my own scrutinize passes weren't enough.
- [Route F · GOTCHAS.md] An unfamiliar commit's message overclaimed its actual diff (described finished work not yet in the diff) — required `git show --stat` investigation before it was safe to merge to main.
- [Route D · memory] Telling a fix-dispatch subagent to verify a reviewer's suggested snippet (not transcribe it verbatim) let it catch that the suggestion itself was a no-op and ship a correct alternative instead.

**Applied:** all 4 (user approved "all").

**Sources cited:** all findings verified empirically in-session (subagent reports, diff tracing, git history). No external research needed — none were technical/API claims requiring doc verification.

**Outcome:** RHR redesign shipped to `origin/main` (commits `8ff3c8d..ca31cd6`) — `Sensors.todaysRhr()` derives today's RHR from `SensorHistory` instead of the confirmed-unreliable `UserProfile.restingHeartRate`, with a physiological floor and pre-wake guard (both `/scrutinize`-found, one pre-existing in `bodyBatteryAtWake()` too and fixed as a same-session follow-up), `DailyRecord`/`RecordStore` carry the new `:rhrBpm` field with backward compatibility, `NowView` reuses the stored value. 38/38 tests passing.
